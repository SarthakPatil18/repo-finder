# GitHub Personal Access Token (Classic or Fine-grained)
# Used to increase rate limits (Search: 30 req/min, REST: 5,000 req/hour).
# Optional for light usage, but recommended for large scans.
GITHUB_TOKEN=
